// 开发环境配置
// ====== API服务器地址（部署时只改这一行）======
const baseApi = "http://api.heibbs.net:8081/api"
// =============================================
const avatarUrl = `${baseApi}/attachment`
const imgUrl = baseApi

export default {
    imgUrl, baseApi, avatarUrl
};